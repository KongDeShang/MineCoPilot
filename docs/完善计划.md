# 矿山智工 · 完善计划（v1.1.x 收敛轮）

> **定位：不扩建。** 本轮不加新功能域，只做一件事——**让「已经宣称的能力」与代码一致**，并补上结构性缺口。
>
> **决策（2026-09-25）**：`docs/升级规划.md` 中的任务 10（数据大屏）、11（多视图）、12（Ctrl+K 命令面板）、13（报告导出增强）、15（语音纪要域）**全部搁置**；模型能力分级**先收敛口径、暂不接线**。
>
> **三条铁律不变**：运行期零联网 / 数据不出本机 / 数字不变量校验。

---

## 一、判据

本轮的验收标准不是"加了什么"，而是这一条：

> **声明的接口没有消费方就不算完成，只算占位。**
> —— 项目自己立的规矩，见 `src/renderer/src/domains/README.md`

按这条判据过一遍当前代码，分两类处置：

- **让它成真** —— 声明有真实价值、代价可控（如手册检索截断、错误边界）
- **把声明改成真的** —— 声明无消费方、或兑现代价远超收益（如模型能力分级、引导演示文案）

两者都是"完善"，且第二类往往更急——**界面上一句没有代码支撑的话，就是把「可审计 AI」的立身之本折价。**

---

## 二、范围

### 做

| 方向 | 内容 |
|------|------|
| 如实化 | 界面/引导文档里没有代码支撑的表述，改到与实现一致 |
| 真缺陷 | 功能上确实坏了或静默失效的路径（手册检索截断、UI 层吞异常） |
| 韧性 | 错误边界；出错时能看见、能恢复，而不是白屏 |
| 卫生 | 孤儿注释、死导出——回到项目自己定的清理标准 |
| 结构债 | 撤销持久化、日志全量化、分页/虚拟滚动、检索性能 |
| 验证网 | 补上视觉回归自动化，让"靠人过截图"这一层也有基线 |

### 不做

- 升级规划任务 10 / 11 / 12 / 13 / 15（大屏、多视图、命令面板、报告导出、语音纪要）
- 模型能力分级**接线**（本轮只收敛口径；接线作为后续可选项，见 §六）
- 升级规划已"明确不做"的方向：模型微调、传感器接入、局域网多端协同、移动端/扫码、数字孪生/3D、预测性维护

### 三条铁律的自检

本轮所有改动**不新增任何对外网络请求、不引入云端依赖、不绕过 `verifyNumbersSubset()`**。唯一涉及网络的是既有模型下载路径，不动。

---

## 三、批次总览

| 批次 | 主题 | 项数 | 性质 | 风险 | 预估 |
|------|------|------|------|------|------|
| 第一批 | 止血与如实化 | 8 | 低风险、逐项独立可验 | 低 | ~3 人天 |
| 第二批 | 数据与可审计 | 5 | 触及核心（撤销栈 / 日志 / 检索） | 中 | ~7 人天 |
| 第三批 | 体验与可信度收尾 | 4 | 独立、可并行 | 低-中 | ~5 人天 |

**每项独立提交**，提交前跑对应验收；涉及业务逻辑的跑全量 `npm run verify`。

---

## 四、第一批 · 止血与如实化

> 目标：半天到一天内把"说错的话"和"静默失效的路径"清掉。这一批全部低风险，且多数改的是文案与常量。

### P1-1 引导演示文案如实化

**证据**：`src/renderer/src/utils/demoRoutes/ai-line.js:40-45`

```
"按住说话自动转工单；拍一张手写巡检表，自动识别成结构化文字。这两条录入通道是给机修现场设计的。"
"轻量 / 标准 / 增强三档……同一套对话界面背后是不同的推理能力。"
```

**现状**：口述是写死字符串（`AIAssistant.vue:592`）、OCR 是写死文本（`:605`），界面上挂着「演示样例」角标，README 也如实承认。**引导文案把它当真实能力讲**——评委跟着引导走，看到的是宣传口径，点进去看到的是"演示样例"角标。

**改法**：
- 口述/OCR 步骤文案改为如实表述，并**主动点出角标**（把弱点讲成"我们知道边界在哪"）
- 三档描述去掉"不同的推理能力"——实际只有叙述长度与上下文窗口不同
- 顺带核对其余路线（主线 / 大屏线）文案，凡涉及未实现能力的同样处理

**落点**：`utils/demoRoutes/ai-line.js`、`main.js`、`big-screen.js`
**验收**：`npm run e2e`（引导演示有断言）+ 人工走一遍 AI 线
**工作量**：0.5 h ｜ **风险**：无

---

### P1-2 手册检索截断修复 + 如实标注

**证据**（三处拼起来才是全貌）：

| 位置 | 内容 |
|------|------|
| `utils/docService/adapters/pdf.js:31` | 切片粒度 = **一页一片**（`chunks.push({ page: p, text })`） |
| `stores/documentDomain.js:34` | `CHUNK_LIMIT = 300`，入库保留前 300 片 |
| `stores/appStore.js:1058` | 检索池只取 `doc.chunks.slice(0, 120)` |
| `views/Documents.vue:29` | 界面显示 `可问答（${row.pages} 页）`——用的是 **PDF 总页数** |

**现状**：**超过 120 页的手册，第 121 页起的正文在 AI 问答里永久检索不到**，而界面上按 PDF 总页数显示"可问答（300 页）"，与实际可检索范围不符，用户无从察觉。两个常量不一致（300 vs 120）本身就是漏改痕迹。随包 3 本手册是 36/32/18 页，全在 120 以内，**演示永远暴露不出来**；导入《资料/》里的真手册（XGT7528A 16.8 MB、XE230_XE250C 12.4 MB）即失效。

**改法**：
1. 检索池取**全量已存切片**（去掉 `slice(0, 120)`，与 `CHUNK_LIMIT` 对齐）
2. 入库时记录 `indexedChunks` 与 `truncated` 标记
3. 界面用**可检索页数**，截断时如实标注"仅前 N 页可问答"

**落点**：`stores/appStore.js`、`stores/documentDomain.js`、`views/Documents.vue`
**验收**：新增 self-check 断言——构造 >120 片的手册，断言全量进检索池、截断标记正确；e2e 手册库文案
**工作量**：0.5 天 ｜ **风险**：低（检索池变大，注意 P2-3 的性能项）

---

### P1-3 全局错误边界

**证据**：全仓 `app.config.errorHandler` / `onErrorCaptured` / `window.onerror` **零命中**。

**现状**：README 自己写着"模板里调用一个没导入的函数会导致整页白屏"，而防线只有 `vue/no-undef-properties` 一条**静态**规则。运行期任何渲染异常（脏数据、越界取值）都会白屏——**无提示、无日志、无恢复入口**。对现场用户就是"软件坏了"。

**改法**：
- `renderer/src/main.js` 加 `app.config.errorHandler` + `router.onError` + `window.onerror` / `unhandledrejection`
- 错误写入操作日志（复用 `addLog`，`silent` 避免递归）
- 界面给**可恢复**的提示（"这一页出错了，返回看板"），而不是白屏
- ⚠️ 处理器自身不得抛错（否则死循环）

**落点**：`renderer/src/main.js`、可能的 `App.vue`
**验收**：store-check 断言错误处理器存在且能落日志（DOM 层行为由 e2e 观察）
**工作量**：0.5 天 ｜ **风险**：低

---

### P1-4 孤儿 JSDoc 清理

**证据**：`stores/appStore.js:1040-1050` 三块注释**下面没有任何函数**：

```js
/** 添加文档：保存文件 → 提取文本 → 登记元数据 … */
/** 删除文档：删记录 + 删文件字节 */
/** 用系统阅读器 / 新窗口打开文档原文 */
```

**现状**：实现早已搬去 `stores/documentDomain.js`（`addDocument` / `removeDocument` / `openDocument`），注释留在 store 里。**注释层在说谎**——读代码的人会以为这里还有实现。

**改法**：删除三块注释；在文档域的位置补一句"实现见 `stores/documentDomain.js`"（若 store 里仍需要指路）

**落点**：`stores/appStore.js` ｜ **验收**：`npm run lint` + `store-check` ｜ **工作量**：10 min ｜ **风险**：无

---

### P1-5 死导出处置

**证据**（全仓 grep，含 `scripts/`，确认零调用方）：

| 位置 | 导出 | 说明 |
|------|------|------|
| `utils/health.js:629` | `isWorsening()` | `evaluateTrend` 的一行包装；Dashboard 读的是 `trend.worsening` 字段 |
| `utils/pdfExtract.js:18` | `keywordsFromDoc()` | 无人调用 |
| `utils/synonyms.js:94` | `getSynonyms()` | 无人调用；同文件 `expandQuery` 重写了一套内联逻辑 |
| `utils/synonyms.js:141` | `isRelated()` | 无人调用 |
| `utils/typewriter.js:147` | `createTypewriter()` | 无人调用 |

**现状**：项目在 2026-09-17 那轮明确立过判据并清理过同类问题，**现在又攒了 5 个**。

**改法**：一律删除。

> ⚠️ **`health.js` 在项目的"不动清单"上（`只增不改`）。** 删除一个零调用方的导出不可能造成回归，但**确实与清单字面冲突**——此项**需你点头**后再动；若你希望严格守规矩，可只删其余 4 个，`isWorsening` 单独保留并在注释里标明"无消费方"。

**落点**：5 个 utils 文件 ｜ **验收**：`npm run lint` + 全量 `verify` ｜ **工作量**：0.5 h ｜ **风险**：低

---

### P1-6 模型能力口径收敛

**证据**：

- `main/ModelRegistry.js:26/39/52` 给三档声明 `narrate / diagnose / summarize / reason`
- **消费方只有两处界面角标**：`views/ModelHub.vue:100`、`components/ModelWizard.vue:37`
- `llmGenerate` 的调用方只有 `utils/narrate.js:117`（叙述）与 `ModelHub.vue:425`（自测）——**diagnose/summarize/reason 零调用点**
- `main/modelManager.js:385` 对增强档抛"暂无可用下载地址（待扩展）"

**现状**：界面上写着标准档"可分析故障模式并给出初步诊断"——**这句话没有任何代码支撑**；下载 1.04 GB 的 1.5B 档，唯一变化是叙述更长。增强档"云端待扩展"，实际永远拿不到。任务总览把任务 06 标为 ✅，但里程碑 M3 未做。

**改法**（**收敛口径，不接线**）：
- 能力标签区分**已启用**（叙述）与**预留**（诊断/摘要/推演），一眼可辨
- 档位描述改到与实现一致：只说"叙述长度与上下文窗口不同"
- 增强档明确标"预留，暂无云端下载源"
- 首启向导（`ModelWizard.vue`）同口径

**落点**：`views/ModelHub.vue`、`components/ModelWizard.vue`（必要时含 `main/ModelRegistry.js` 的 `description`）
**验收**：`npm run e2e` + 人工过模型页与首启向导
**工作量**：0.5 天 ｜ **风险**：无

---

### P1-7 撤销按钮状态修正

**证据**：`components/ChatMessage.vue:127` 无条件渲染按钮（`v-else` 只判 `undone`）；`views/AIAssistant.vue:1090` 在**点击后**才弹警告说明刷新后不可撤。

**现状**：诚实，但用户感知更接近 bug——"按钮是亮的，点了告诉我不能用"。（评审报告 P0-2 建议置灰 + tooltip。）

**改法**：卡片已有 `session` 字段判断是否本次页面加载写入的；非本次会话 → `disabled` + tooltip 说明原因，把"诚实的拒绝"变成"诚实的外观"。

**落点**：`components/ChatMessage.vue`、`views/AIAssistant.vue`
**验收**：`npm run e2e:nl` 补一条"刷新后撤销按钮为 disabled"
**工作量**：0.5 天 ｜ **风险**：低

---

### P1-8 UI 层静默吞异常区分

**证据**：`KnowledgeBase.vue:216/233`、`RecheckManage.vue:106`、`WorkOrder.vue:556`、`ModelWizard.vue:165`、`main/index.js:299`——共 6 处 `.catch(() => {})`。

**现状**：多数吞的是 `ElMessageBox` 用户点取消时的 reject（**正常写法，不算缺陷**）。但**`.then()` 体内的写库调用若抛错，也会被一并吞掉**。项目在 store 层做得极严（落盘失败必须抛错、界面必须报"保存失败"），UI 层这几处把这套严格性漏掉了。

**改法**：区分两类——取消保持现状；业务体用 try/catch 包住，失败走 `ElMessage.error` + `console.error`。

**落点**：上述 4 个 vue + `main/index.js`
**验收**：`npm run lint` + `e2e` ｜ **工作量**：0.5 天 ｜ **风险**：低

---

## 五、第二 / 三批（待第一批完成后细化）

### 第二批 · 数据与可审计

| # | 事项 | 要点 |
|---|------|------|
| P2-1 | **撤销事件溯源** | 闭包式撤销 → 可序列化逆操作描述。一次同时解决：跨刷新撤销失效 + 操作日志非全量（事件日志本身就是审计日志）。落点 `stores/nlActions.js` |
| P2-2 | **操作日志补全** | 备件出入库、设备新增/编辑、复诊标记这几类补记日志；确认 `LOG_WINDOW` 口径统一 |
| P2-3 | **检索性能** | `answerItems` 是全量 computed（知识条目 + 每本手册全部切片），每次提问重建。改切片池缓存 + 内容分增量 |
| P2-4 | **分页 / 虚拟滚动推广** | 全仓 `el-pagination` 只在导入预览里；虚拟滚动只有设备台账。工单/病历/备件表接入 |
| P2-5 | **单机规模压测** | 生成 5000 台设备 + 5 万条维保记录，实测首启/落盘/看板/检索延迟，把数据写进 README 与答辩材料 |

#### 细化（2026-09-25 定稿）

**P2-1 撤销持久化** —— 落点 `stores/nlActions.js`（主）、`utils/nlCommand.js`、`views/AIAssistant.vue`、`components/ChatMessage.vue`

> ⚠️ **不动清单冲突已裁决**：逆操作描述只能由 `executePlanItem` 产出，而该文件在"只增不改"清单上。2026-09-25 用户明确授权：**只增 `inverse` 字段，不改判定逻辑**（`undo:` 闭包原样保留）。

现状（实测）：撤销栈是 `appStore.js:1196` 的内存 `ref([])`，条目 `{rawText, summary, changes, undos(闭包数组), at, id}`；`id` 是 `undo-N` 的**每页重排**计数器；跨刷新失效靠 `AIAssistant.vue:254` 的 `CHAT_SESSION_ID` 拦（第一批已把它变成"置灰 + tooltip"，但能力本身仍然只在当次加载内可用）。

六类 `undo:` 闭包（`nlCommand.js`）→ 逆操作描述：

| 意图 | 现闭包 | 逆操作描述 |
|------|--------|-----------|
| REPORT_FAULT (:833) | `removeWorkOrder(id)` | `{op:'remove', table:'work_orders', id}` |
| COMPLETE_ORDER (:873) | 还原工单字段 + 删本次案例/知识条目 + 还原设备快照 | `update(work_orders, id, before, after)` + `remove_many(fault_cases, ids)` + `remove_many(knowledge_items, ids)` + `restore_equipment(snapshot)` |
| ADD_MAINTENANCE (:910) | `revertConsumption(applied)` + 还原设备快照 | `revert_consumption(applied)` + `restore_equipment(snapshot)` |
| SET_STATUS (:930) | `updateEquipment(id,{status:before})` | `update(equipment, id, before, after)` |
| DO_RECHECK (:942) | `updateWorkOrder(id,{recheck_status:'pending'})` | `update(work_orders, id, before, after)` |
| ADD_KNOWLEDGE (:962) | `removeKnowledgeItem(id)` | `{op:'remove', table:'knowledge_items', id}` |

三条设计要点：

1. **带 `after` 做 CAS**。跨刷新撤销的真正危险不是"撤不了"，而是"这条记录之后被别人改过，撤销却照旧把 before 盖回去"（静默改错数据，与 2026-09-17 修的"撤销栈不清导致改错记录"同类）。因此 `update` 类逆操作同时记 `after`，撤销前比对当前值：不等就**拒绝**并说明，不自动回退。
2. **闭包与描述两条路必须给出同一结果**。闭包是当次生效路径、描述是持久化路径，两份实现最容易悄悄漂移 —— 用断言锁住：同一场景分别走两条路，比对结果状态（变异：摘掉 `inverse` 必红）。
3. **持久化预算**。`restore_equipment` 快照含设备整行 + 全部维保记录 + 全部健康快照，单条可达十几 KB。栈上限 20 条写进 localStorage 需要设预算（超预算从栈底淘汰并**在日志里明说"最早 N 条不再可跨刷新撤销"**，不静默丢）。`clearUndoStack` 必须同时清持久化副本 —— 导入备份 / `resetToSeedData` 都会整库重建、id 全部重排，留着的旧栈会去改重排后的记录。

顺带修一处同源缺陷：`performUndo` 现在对每个子步骤 `catch { console.warn }`，失败被吞掉后仍返回 `{ok:true, reverted:N}` —— 部分失败在界面上完全看不见。改为返回 `{ok, reverted, failed:[{why}]}` 并由卡片如实显示。

**P2-2 操作日志补全** —— 落点 `stores/partsDomain.js`、`stores/appStore.js`、`stores/nlActions.js`

实测缺口（2026-09-25 全仓盘点）：备件**入库 / 出库 / 调整 / 新增**四条写入路径**全都没有日志**（含 `views/PartsInventory.vue` 的三个 UI 入口）；设备**新增 / 编辑**无日志；复诊**「无需复诊」**（`markRecheckNotNeeded`）无日志。

口径要统一：**日志落在 store 的写入口，而不是各 UI 入口各补一条**。现存的"UI 层补日志"已经漏了实例 —— `store.addWorkOrder` 本身无日志，靠三个 UI 入口各补一条，于是 `recheckFailedAndReopen` 内部那次建单就没有独立日志。

**P2-3 检索性能** —— 落点 `stores/appStore.js` 的 `answerItems`

现状是全量 computed（知识条目 + **每本手册的全部切片**）每次提问重建；P1-2 刚把检索池从 `slice(0,120)` 放开到全量，成本只会更高，因此这一项在第一批之后**更必要而非更不必要**。改法：切片池按内容分缓存（`documents` 变更时失效），逐条打分增量计算。

**P2-4 分页 / 虚拟滚动推广** —— 落点 `views/WorkOrder.vue`、`views/PartsInventory.vue` 等大表

现状：全仓 `el-pagination` 只在导入预览里，虚拟滚动只有设备台账。与 P3-1（窄屏推广）是同一类"只有一张表享受到了"的债。

**P2-5 单机规模压测** —— 新建 `scripts/bench-scale.mjs`

`scripts/` 下目前**没有任何性能基座**。脚本生成 5000 台设备 + 5 万条维保记录，实测首启 / 落盘 / 看板 / 检索延迟，结果写进 README 与 `docs/测试报告.md`。**不进 `npm run verify` 门禁**（它是耗时测量，不是断言；偶发的性能抖动不该让整条验收链变红），单独给一个 npm 目标。

### 第三批 · 体验与可信度收尾

| # | 事项 | 要点 |
|---|------|------|
| P3-1 | **窄屏响应式推广** | 当前只有工单/备件两张表接入 `utils/responsive.js`，其余 13 张表仍是横向滚动 |
| P3-2 | **视觉回归自动化** | Playwright 截图基线（深浅双主题 × 16 路由）。README 已承认"靠人过截图"，而历史上 3 个高严重度缺陷都是这样漏过去的 |
| P3-3 | **浏览器模式导入备份修复** | `views/Settings.vue:436` 调用 `importBackup()` 未传文件内容，浏览器模式下必然失败（README 已知边界） |
| P3-4 | **手册批量导入**（可选） | 桌面版从文件夹批量导入。**依赖 P1-2 先修好**，否则导入也是"前 N 页能查、后面查不到" |

---

## 六、后续可选项（本轮不做，记录备查）

**模型能力分级接线** —— 若日后有余力，最小可用形态：

给 `diagnose` 能力接一个入口：AI 助手里"这个故障可能是什么原因"的追问，在标准档以上走本地 LLM 生成候选原因，**输出强制过既有确认卡 + `verifyNumbersSubset()`**，档位不够则隐藏入口并说明"需标准档以上"。

这样下载 1.5B 的收益才从"叙述更长"变成"能问诊断"，且安全叙事完全保留（LLM 仍不碰数字、仍要人确认）。**本轮已决定不做，仅记录。**

---

## 七、验收纪律

每批、每项完成后：

```bash
npm run lint          # 静态检查
npm run self-check    # 纯逻辑断言
npm run main-check    # 主进程契约
npm run store-check   # store 行为
npm run coverage      # 指代消解覆盖矩阵
npm run build         # 生产构建
npm run e2e           # 端到端
npm run e2e:nl        # 口述录入端到端
npm run audit:contrast        # 浅色对比度
npm run audit:contrast:dark   # 深色对比度
```

**两条纪律**（沿用 2026-09-17 那轮的做法）：

1. **每条新增断言都要做"能失败"验证** —— 把缺陷放回去，断言必须变红并给出精确诊断。不满足这条的断言不算数。
2. **`verify` 全绿不等于界面没问题** —— 视觉缺陷（遮挡、`display:none`、压根没渲染）在 DOM 层都是"存在、文案正确"。第一批的 P1-2 / P1-6 / P1-7 都涉及界面文案与状态，**必须人工过截图**，不能只看断言。

---

## 八、进度

| 批次 | 状态 | 完成时间 |
|------|------|---------|
| 第一批 | ✅ 已完成（8/8） | 2026-09-25 |
| 第二批 | 🚧 进行中（1/5，P2-1 已完成） | 2026-09-25 |
| 第三批 | ⬜ 未开始 | — |

### 第一批逐项

| 项 | 状态 | 实际落点与偏离 |
|----|------|----------------|
| P1-1 引导演示文案如实化 | ✅ | `demoRoutes/ai-line.js` 三处（口述/OCR 点出「演示样例」角标、三档去掉"不同推理能力"）；`demoRoutes/main.js` 四因子名订正为与 `health.js` 一致（维保及时性/设备机龄/故障状态/数据完整度）；顺带删掉 `big-screen.js`（`pending:true` + 空 steps，全仓唯一的 TODO 标记），`index.js`/`App.vue` 去掉 `pending` 分支 |
| P1-2 手册检索截断修复 | ✅ | `appStore` 去掉 `slice(0,120)`；新增 `documents.chunk_total` 列（迁移 + 行映射 + 读回）+ `Documents.vue` 标黄三态。**验收**：store-check +6 条，含落盘往返 |
| P1-3 全局错误边界 | ✅ | 新增 `utils/errorBoundary.js`（组件 / 路由 / window / 未处理 Promise 四条入口）+ `App.vue` 提示条 + `main.js` 接线。**超出原计划**：store-check +14 条、e2e +9 条（原计划只要 store-check） |
| P1-4 孤儿 JSDoc 清理 | ✅ | `stores/appStore.js` 三块无函数体的 JSDoc → 一行注释指向 `documentDomain.js` |
| P1-5 死导出处置 | ✅ | `getSynonyms`/`isRelated`/`createTypewriter` 已删；`pdfExtract.js` **整文件**删除（两个导出都零调用方，不止 `keywordsFromDoc`）；`isWorsening` 经用户授权后**也已删除**（见下方已裁决） |
| P1-6 模型能力口径收敛 | ✅ | `ModelRegistry` / `ModelHub` / `ModelWizard` / `demoRoutes` 收敛为"只有叙述已接线，其余标注（预留）" |
| P1-7 撤销按钮状态修正 | ✅ | `ChatMessage.vue` 置灰 + tooltip（必须套 `<span>`，否则 disabled 的按钮收不到 hover）+ 副标文案；`AIAssistant.vue` 加 `canUndoMsg`（外观一道、执行前一道）。**验收**：e2e:nl 成对 3 条 |
| P1-8 UI 层静默吞异常区分 | ⚠️ **超出原计划** | 新增 `utils/confirmAction.js` 把"取消 vs 失败"下沉到一处；4 个 vue 改走它；`ModelWizard.startDownload` 原本**连 catch 都没有**（IPC 失败会永久卡在"下载中"）；`main/index.js` 的 `docs:restoreAll` 改为如实上报"旧文档没清掉"，并连带修正 `utils/backup.js` → `Settings.vue` 那句无条件的"文档资料已替换为备份内容" |

**新增断言总量**：store-check 20 → 40（+20），e2e 116 → 125（+9），e2e:nl 28 → 31（+3）。

每条新断言都做过"能失败"验证（把缺陷放回去必须变红）：

- P1-2 三种缺陷各验一次：检索侧恢复 `slice(0,120)`（3 红）、`documentsToRows` 漏 `chunk_total`（1 红）、读映射忽略该列（1 红）。
- P1-3：删掉 `main.js` 里那行接线 → e2e 7 条红。这条正是 store-check 看不见的那一层。
- P1-7：不再传 `undoable` → e2e:nl 2 条红。

### 第二批逐项

| 项 | 状态 | 实际落点与偏离 |
|----|------|----------------|
| P2-1 撤销事件溯源 | ✅ | `nlCommand.js` 六类意图各加 `inverse` 描述（`remove`/`remove_many`/`update`/`restore_equipment`/`revert_consumption`，闭包原样保留）；`nlActions.js` 撤销栈落本机存档 `ks:undo-stack`（`UNDO_BUDGET` 512 KB + 上限 20 条，超预算**从最早的淘汰并写日志**）、`performUndo(entryId)` 按 id 撤、`hasUndo(entryId)` 取代 `CHAT_SESSION_ID` 页面加载期闸门、`update` 类逆操作带 `after` 做 CAS（写入后又被改过**拒绝**自动回退）；`appStore.js` **三处整库重建**都清栈（导入备份 / 恢复演示数据 / 播种分支）；`AIAssistant.vue` + `ChatMessage.vue` 卡片状态与文案。**超出原计划**：`storeApi.current` 改为解包视图（见下） |

**新增断言总量**：store-check 40 → 61（+21），e2e 125（本节无变化），e2e:nl 31 → 36（+5）。

每条新断言都做过"能失败"验证（把缺陷放回去必须变红）：

- **六类变异全部验过，逐条给出精确诊断**：①`inverse:` 改名（`nlCommand.js:977`）→ 3 红，诊断"成对 5 / 只有闭包 1"，且 CAS 两条同时变红（闭包兜底把手工改动静默盖回去，正是 CAS 要拦的那件事）；②完成工单的 `before` 收窄成 `{status}` → 1 红，逐字段列出 `archived_at/completed_at/recheck_date/recheck_status/status` 五处漂移；③同一个 `inverse` 的 `after` 清空 → 1 红（"缺 after：complete_order/update"）；④`performUndo` 收尾去掉 `saveUndoStack()` → 1 红，诊断直接列出残留条目 id；⑤淘汰处 `entries.slice(0, keep)` → `entries` → 1 红（大条目 A 没被淘汰）；⑥`resetToSeedData` 去掉 `clearUndoStack()` → 2 红，并 dump 出存档里残留的那条。
- **e2e:nl 第 9 节是第 4 节的反面钉子**：把本机存档删掉再刷新 → 撤销按钮必须置灰。没有它，一个把 `undoable` 写死成 `true` 的实现照样能让"刷新后仍可按"全绿。
- **被测出来的真缺陷（正是这一节想抓的）**：`storeApi.current = api` 把 Pinia 的 ref 直接交给了执行器，`(list || []).find is not a function`。auto-unwrap 只发生在 store 实例上，不会发生在裸对象上 —— 修成 `storeApiView`（getter 视图，getter 也必须是活的而不是快照）。

**同批修掉的既有断言缺陷**（不是产品缺陷，是断言本来就不该那么写）：

- P1-7 在 e2e:nl 的三条成对断言（"刷新后置灰"）**行为已反转**，改写为"刷新后仍可按 + 提示写明会恢复写入前数据 + **真的点下去按条数验回滚**"，另补第 9 节做反面钉子。
- 原第 7 节"口述'已检修完'"断言钉死了某一张工单 id，只在"该设备恰好只有这一张未完成工单"时成立 —— 演示台账里一台设备带 2~3 张未完成工单是常态，钉 id 等于把"设备唯一"这个前提偷塞进断言，库一脏（反复跑 e2e 用的是同一个 profile）就随机红。改为**设备级语义**：该设备未完成工单数 −1，且新收掉的那张挂上 7 天内复诊、复诊任务 +1。

### 未做 / 已裁决（2026-09-25 落档）

1. **`utils/health.js` 的 `isWorsening()` —— 已删除**（用户授权）。零调用方（全仓 grep 含 `scripts/`、`docs/` 确认），且原注释那句"供 Dashboard 汇总"不实：Dashboard 读的是 `evaluateTrend` 返回对象上的 `trend.worsening` 字段，从没调过它。`health.js` 在不动清单上（只增不改），**本次删除属用户单独授权**，已同步删掉那段"当前无消费方"的说明性 JSDoc（留着就成了 P1-4 要清的孤儿 JSDoc）。
2. **`utils/pdfExtract.js` 整文件删除 —— 维持**（用户确认不回滚）。P1-5 原本只点了 `keywordsFromDoc`，实测 `extractPdfText` 同样零调用方 —— 这个"兼容层"从未产生过调用方（迁移时就直接改的调用方），留着等于一个空壳。已整文件删除，并订正 `docs/tasks/08-统一文档解析.md` 里"保留旧导出名"的记载。

> 由此确立一条口径（下次盘点照此执行）：**不动清单保护的是"已验证的判定逻辑"，不是"导出面"。**
> 零调用方的导出一律清掉，不必逐个再问；只增不改约束的是计算与规则部分。
