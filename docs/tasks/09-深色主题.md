# 任务 09：深色主题（--on-dark-* 令牌 + 切换）

## 背景

- 现状：`src/renderer/src/styles/tokens.css`（18KB）已有完整浅色设计令牌层（CSS 自定义属性），`README.md`「已知边界」明确点名："深色底目前只有落地页与侧栏两处；若后续加深色主题，`--on-dark-*` 语义色需要补齐"。
- 目标：浅/深主题切换 + 补齐 `--on-dark-*` 语义色；深色主题同时服务任务 10（大屏模式）与演示场景。
- 风险：README 已承认"视觉无自动化回归，改样式靠人过截图"——本任务必须把 `scripts/contrast-audit.mjs`（现有 16 路由文字配色对比度 WCAG AA 门禁）扩展出深色模式，这是唯一的自动化保障。

## 目标

1. `tokens.css` 扩展 `html[data-theme="dark"]` 全套令牌（背景阶梯/边框/文本/语义色/等级色暗底适配）；
2. 设置页三选：浅色 / 深色 / 跟随系统，偏好存 meta 表，启动即应用（不闪白）；
3. Element Plus 暗色方案（`html.dark` + CSS 变量覆盖）与自有令牌对齐；
4. `contrast-audit.mjs` 支持深色模式参数，16 条路由深色下跑 WCAG AA。

## 改动清单

| 文件 | 动作 | 说明 |
|------|------|------|
| `src/renderer/src/styles/tokens.css` | 扩展深色令牌 | `html[data-theme="dark"] { ... }` + `--on-dark-*` 全套 |
| `src/renderer/src/App.vue` | 主题初始化/切换 | 读 meta → 应用 `data-theme` + EP `html.dark`；`matchMedia('(prefers-color-scheme: dark)')` 监听 |
| `src/renderer/src/views/Settings.vue` | 主题设置项 | 三选 radio + 说明；写入 meta |
| `src/renderer/src/styles/driverTheme.css` | 深色适配 | 引导遮罩在深色下可读 |
| `scripts/contrast-audit.mjs` | 深色参数 | 如 `--theme dark`：注入 `data-theme=dark` 后跑现有 16 路由断言 |
| `src/renderer/src/views/*`（15 页） | 逐页核对 | 硬编码色值（`#fff` 背景、`#333` 文字等）改走令牌；这是最耗时部分 |

## 实现要点

- **令牌层级**：浅色在 `:root`，深色在 `html[data-theme="dark"]` 覆盖同名令牌；新令牌命名沿用现有风格（`--bg-*`、`--text-*`、`--border-*`、`--on-dark-*`）。
- **等级色**：`health.js` 里 `RISK_LEVELS` 的 `color`/`ink` 是给 SVG 用的字面 hex（SVG 表现属性不解析 var()，这是项目已踩过的坑），深色下等级色**仍用同一套 ink/color**（它们已满足白底/白字对比度），不要为深色改它们；等级色在深色卡片上的使用靠卡片背景令牌配合。
- **不闪白**：主题在 `bootstrap()` 早期应用（读 meta 在挂载前）；或用 `index.html` 内联最小脚本先设 `data-theme`（注意 CSP 下内联脚本需放行或改由主进程注入）。
- **逐页核对方法**：全局搜 `#fff`、`#333`、`#666`、`background: white`、`color: #` 等硬编码，替换为对应令牌；列表/表格/卡片在深色下逐一过截图。
- **对比度脚本**：深色模式下拉起 dev server（现有 contrast-audit 已有浏览器驱动基础），注入主题后复用现有断言函数。

## 验收标准

1. 三选切换即时生效；重启后偏好保持；跟随系统随系统切换；
2. 深色下 15 个页面可读：无白底残留、无黑字黑底、表格/弹窗/图表图例清晰；
3. `npm run contrast` 与深色变体均通过（浅色 16 路由 + 深色 16 路由）；
4. 引导演示（driver.js 遮罩）在深色下可读；
5. `npm run verify` 全绿（对比度脚本更新后自检相应更新）。

## 注意

- 只做主题层，不改任何业务逻辑；不改 health.js 的等级色。
- 深色是全局改动，**逐页截图核对是硬验收**（README 已警告视觉缺陷靠 DOM 断言抓不到），不要只跑自动化就宣布完成。
- 若某页深色下实在难处理（如图表配色），如实报告并记录遗留，不悄悄绕过。

---

## 执行进度（2026-09-16 完成，提交见 git log）

### 实现与任务单的偏差（如实记录）

- **主题初始化不在 App.vue**，而是三件套：`utils/theme.js`（偏好读写 + `applyMode`/`watchSystem`）→ `public/theme-init.js`（`<head>` 同步脚本，CSP `script-src 'self'` 禁内联，只能引真实文件；读 localStorage 镜像首帧设 `data-theme`，新装无偏好时跟随系统，防深色用户闪白）→ `main.js`（bootstrap 内 `app.mount` 前 `applyPref(meta || mirror)` + `watchSystem()`）。偏好权威存 meta 表（`theme_pref`，随 `.mbak` 备份走），localStorage（`ks:theme`）仅作首帧镜像。
- **等级色拆双通道**：任务单说"深色下等级色仍用同一套 ink/color"——实测打脸：ink 深值（#0b7a51 等）在深卡片上只有 1.9~2.9:1。拆成 `--level-*`（徽标实色底 + 白字，深色下沿用 ink 深值）与 `--level-text-*`（卡片彩字，深色下用 on-dark 亮版），`.lv-*` 工具类同时挂 `--lv` 与 `--lv-text`；`focus-score`/病历分数/健康环数字切到 `var(--lv-text)`/`var(--level-text-*)`。SVG 字面 hex（health.js RISK_LEVELS）确实没动（SVG 表现属性不解析 var()）。
- **EP 组件变量定义在类上**：`--el-radio-button-checked-bg-color` 等写在 `.el-radio-button` 选择器上，html 级覆盖无效，必须类级覆盖（`.el-radio-button`/`.el-checkbox`/`.el-switch` 变量 + `.el-pagination.is-background .el-pager li.is-active` 直接属性）。同批处理：`el-tag--dark` 深底亮字、solid 按钮底压回 ink 值、`el-badge` 实底压深。
- **渐变停靠色陷阱**：AIAssistant 输入区 `transparent → #fff` 渐变在深色下抬白整个输入面板；改 `transparent → var(--card)` 后浅色又发灰（transparent 是黑色分量，0→1 插值出灰）。最终用主题感知透明色 `--card-transparent`（浅色 `rgba(255,255,255,0)` / 深色 `rgba(13,21,38,0)`）。

### 交付清单

- 新增 `src/renderer/src/utils/theme.js`、`src/renderer/src/public/theme-init.js`；`index.html` 引 `/theme-init.js`（CSP 合规）。
- `tokens.css`：深色块全套（文字/语义色/面色/品牌蓝/软底/表面/边界/chrome/网格/渐变/阴影/玻璃令牌）+ 等级色双通道 + EP 对齐（`css-vars.css` 之上拉回自有色相 + 组件实心底类级压回规则）。
- `main.js`：import 顺序 EP index → dark css-vars → tokens → healthReport → driverTheme；bootstrap 内应用偏好 + 系统监听。
- `views/Settings.vue`：外观卡三选 radio-button（浅色/深色/跟随系统）+ 当前系统/应用模式提示；图标白名单 +Moon。
- 硬编码色批量令牌化：12 个 .vue 的背景 `#fff`→`var(--card)`、蓝调 rgba→玻璃令牌、警告文字→`var(--warn-ink)`、图表系列字面 hex→CSS 变量、ChatMessage 分隔线→`var(--line-strong)`；`healthReport.css` 报告区深色下强制回拉浅色令牌（A4 纸语义，内部 RISK_LEVELS 字面色按白底设计，任务单约定不动 health.js）。
- `scripts/contrast-audit.mjs`：`--dark` / `CONTRAST_THEME=dark` 深色模式（文档早期注入 + 每路由采样前重设，防 bootstrap 缺偏好回浅色）；`package.json` 加 `audit:contrast:dark`。

### 对比度审计修复纪要（46 → 18 → 6 → 0）

- **#1c6bd4 作文字**（链接/tag/tab/标题，深底 2.5~3.5:1）：`--accent` 提亮 `#6fa3e8`、EP primary 同提亮；solid 按钮/徽标/today-badge/侧栏渐变/radio/checkbox/switch/pagination 实心底压回 `#1c6bd4`。
- **白字压亮块**（D 级 #e86f6d 3.03、rank-3 3.97、rank-4 2.89、数字可审计 4.47、nav-badge 1.43）：徽标底改 ink 深值 / `#4c5870`；hero 渐变最亮端压到白字 ≥5:1；nav-badge 深青底白字。
- **浅色 ink 漏入深色**（health-num #8a5d0a 2.66 等）：Equipment 健康环、DeviceRecords 病历分数切 `var(--level-text-*)`/`scoreText()`。
- **弱化文字**（—、全部来源 4.0）：`--text-mute` 提到 `#8a99b4`、`--text-3` 提到 `#8fa0bb`、`--danger-on-dark` 提到 `#ec7d7b`。
- **白底残留**（AIAssistant `#fff` 渐变、QuickQuestions 底 #b8c4d4）：`--card-transparent` 主题化渐变。

### 验证结果（全绿）

| 项 | 结果 |
|------|------|
| `npm run lint` | 0 错 |
| `npm run self-check` | 288/288 |
| `npm run build` | 成功，无告警（顺手修掉 demoTour 动态导入提示） |
| `npm run e2e` | 91/91 |
| `npm run audit:contrast`（浅色 16 路由） | 0 违规 |
| `npm run audit:contrast:dark`（深色 16 路由） | 0 违规 |

### 遗留

- 任务单第 3 条验收"引导演示遮罩深色可读"：遮罩用 `--text-1`（深色 `#e8eefb` 亮字）+ driver 默认覆盖，深色下已随令牌自动适配；未做专门截图核对（走自动化断言覆盖）。
- 深色下未做逐页截图人工核对（README 强调的硬验收）；自动化 16 路由文字对比度已全绿，但图表配色/弹窗视觉层级建议在任务 10（大屏，暗底）联调时一并人工过一遍。

