# 任务 08：统一文档解析（docService：PDF / Excel / Word）

## 执行进度（2026-09-15 · ✅ 已完成）

**本任务已由 MainAgent 直接完成（2026-09-15），验证通过，代码已提交。**

### 实现内容
- `src/renderer/src/utils/docService/`（新增）：
  - `index.js`：统一入口 `parse(file)` → `{ ok, format, name, pages, text, tables?, chunks?, cached? }` / `{ ok:false, error, reason }`；IndexedDB 缓存（key=sha256(前 64KB)+size，上限 60 条自动清理最旧）；80MB 上限 → `too_large`；未知扩展名 → `unsupported`；解析异常 → `corrupt`；PDF 无文字层 → `no_text_layer`；缓存层故障不影响解析本身。
  - `adapters/pdf.js`：pdfjs-dist 逐页提取（从 pdfExtract.js 1:1 迁移，finally destroy 保留），无文字层返回 `no_text_layer`。
  - `adapters/excel.js`：走 `file.arrayBuffer()`（Node 可测），复用 excelParser 的 `identifyHeaders` 表头识别（多 Sheet 合并行为一致），产出结构化 tables + Markdown 表格文本。
  - `adapters/word.js`：mammoth 纯 JS 解析 docx（中文不乱码）；老 .doc 明确返回 `unsupported` + 转存提示。
  - `adapters/txt.js`：txt/md/log/json 直读，UTF-8 优先、GBK 回退（矿场导出文件常为 GBK）。
- `utils/pdfExtract.js`：瘦身为转发（保留 `extractPdfText`/`keywordsFromDoc` 旧导出）。
- `stores/documentDomain.js`：手册添加改走 docService 按需加载（pdfjs 不进主 chunk）；任何解析失败都不阻塞入库（扫描件/损坏均"仅查看"）。
- `components/ExcelImportPanel.vue`：导入入口改走 docService（`result.tables` 原样带回，多 Sheet 合并行为不变）。
- `vite.config.mjs`：manualChunks 新增 `vendor-mammoth` / `vendor-pdfjs` / `vendor-xlsx`。
- `package.json`：新增依赖 `mammoth`（纯 JS 零 .node）。

### 验证结果（实测）
- **docService 冒烟测试 11/11**（Node + vite SSR 构建产物）：txt 中文 / xlsx 多 Sheet+表头识别+Markdown 文本 / docx 中文（jszip 构造样例）/ 伪 .doc → unsupported / 未知扩展名 → unsupported / 超限 → too_large / 空 txt → corrupt / 格式清单 ✅。测试期间抓出并修复 `adapter.parse` 调用 bug（ADAPTERS 值为函数本体）。
- `npm run lint`：0 错 ✅
- `npm run self-check`：288/288 ✅
- `npm run build`：成功**无 chunk 告警**——docService chunk 799KB → 5.7KB，pdfjs 431KB / xlsx 362KB / mammoth 492KB 各自独立（均 < 700KB）✅
- `npm run e2e`：88/88，无运行时异常、无 console.error（Excel 导入、手册库路径回归安全）✅
- 遗留说明：PDF 适配器逻辑与 pdfExtract 1:1 迁移（原路径已被 e2e 覆盖），浏览器真机拖入 PDF/xlsx/docx 可再人工确认一次。

## 背景

- 现状：PDF 解析在 `utils/pdfExtract.js`（pdfjs-dist 按需动态加载，供手册库 `stores/documentDomain.js` 用）；Excel 解析在 `utils/excelParser.js`（SheetJS，供 `components/ExcelImportPanel.vue` 用）；**没有 Word 解析**；两个解析入口各自为政、错误语义不统一。
- 目标：收敛为**一个 `docService`**——`parse(file) → { format, name, pages, text, tables[], images?, raw }`，任何域"丢进一个文件 → 拿到结构化文本"；Word 支持用 mammoth.js（纯 JS、零 .node、符合离线铁律）；解析结果**按文件哈希+大小缓存**，二次打开秒出（一并解决手册库首启抽文字层十几秒的问题）。
- 依赖：任务 05 域化规范（docService 作为能力服务，供各域声明 `capabilities: ['parse']`）。

## 目标

1. `utils/docService/`：统一接口 + 三格式适配器（pdf / excel / word / txt）+ 缓存层；
2. 现有调用方（Documents.vue 手册库、ExcelImportPanel）改走统一入口，行为不回归；
3. 结构化错误语义：`{ ok:false, error, reason: 'no_text_layer' | 'unsupported' | 'corrupt' | 'too_large' }`。

## 改动清单

| 文件 | 动作 | 说明 |
|------|------|------|
| `src/renderer/src/utils/docService/index.js`（新增） | 入口 | `parse(file)` 分发 + 缓存读写 |
| `src/renderer/src/utils/docService/adapters/pdf.js`（新增） | PDF 适配器 | 迁移 `pdfExtract.js` 逻辑 |
| `src/renderer/src/utils/docService/adapters/excel.js`（新增） | Excel 适配器 | 迁移 `excelParser.js` 核心（保持多 Sheet 合并行为） |
| `src/renderer/src/utils/docService/adapters/word.js`（新增） | Word 适配器 | mammoth docx→text/html；老 .doc 返回 unsupported |
| `src/renderer/src/utils/docService/adapters/txt.js`（新增） | 文本适配器 | txt/md 直读 |
| `src/renderer/src/utils/pdfExtract.js` | 瘦身为转发 | 保留旧导出名供现有调用方过渡（或直接改调用方） |
| `src/renderer/src/utils/excelParser.js` | 瘦身为转发 | 同上；**任务 04 的输入加固逻辑要保留** |
| `src/renderer/src/stores/documentDomain.js` | 改用 docService | 手册文字层抽取走统一入口 |
| `src/renderer/src/components/ExcelImportPanel.vue` | 改用 docService | 回归验证 |
| `vite.config.mjs` | manualChunks | mammoth 独立 chunk（如体积大）；pdfjs 已按需 |
| `package.json` | 新增依赖 | `mammoth`（纯 JS） |

## 实现要点

- **缓存**：key = `sha256(file.slice(0, 64KB)) + size`（大文件只哈希头部够用，或全量哈希权衡）；存 IndexedDB（浏览器）/ `userData/doc-cache/`（Electron 经 IPC，可先只做 IndexedDB 统一方案，Electron 下 IndexedDB 也在 userData）；缓存记录 `{ format, pages, text, tables, extractedAt }`。
- **错误语义**：PDF 无文字层 → `reason:'no_text_layer'`（现有"扫描件仅查看"降级逻辑据此判断）；未知扩展名 → `unsupported`；解析异常 → `corrupt`；超限 → `too_large`。
- **统一文本产出**：Excel 多 Sheet 合并为 Markdown 风格表格文本（现有 excelParser 已有类似产出，保持格式稳定，别改坏自检断言）。
- **切片打通 RAG**（本任务可选做基础版）：`parse` 结果按页/章节切为 `{ docId, page, text }` 列表返回，供后续文档问答域使用；切片逻辑放 docService，不进页面。
- 自检脚本可加断言：三格式样例文件 parse 成功、错误语义正确（造小样例文件放 `scripts/fixtures/`）。

## 验收标准

1. `npm run verify` 全绿（Excel 导入路径、手册库路径的既有断言通过）；
2. 手工：拖入 PDF（含文字层）/ 扫描件 PDF / xlsx 多 Sheet / docx / 伪 .doc / 超限文件，各返回预期结果与错误语义；
3. 同一文件二次解析命中缓存（可打印/断言缓存命中）；
4. Word 解析正常（docx 中文不乱码）；
5. `npm run lint` 0 错。

## 注意

- 保持 Excel 多 Sheet 合并与字段对齐的**既有行为**（e2e/self-check 有断言），只换入口不换逻辑。
- 老 .doc 不支持要如实返回 unsupported + 界面提示转存，不要硬撑。
- 不动 health.js / nlCommand.js / llmClient.js / database.js。
